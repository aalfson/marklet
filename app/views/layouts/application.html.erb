<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Marklet" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
    <%= javascript_include_tag "application" %>
	<script type="text/javascript">

	  var _gaq = _gaq || [];
	  _gaq.push(['_setAccount', 'UA-34051260-1']);
	  _gaq.push(['_trackPageview']);

	  (function() {
	    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
	    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
	    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
	  })();

	</script>
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
		  <%= link_to "marklet", root_url, :class => "brand" %>
          <div class="container nav-collapse">
            <ul class="nav" style='float:right;'>
				<% if user_signed_in? %>
					<li><%= link_to "My Bookmarks", user_url(user: current_user.name), :method => :get %></li>
					<li><%= link_to "Get Bookmarklet", bookmarklet_url(user: current_user.name), :method => :get %></li>
					<li><%= link_to "Account", edit_user_registration_path, :method => :get %></li>
				   	<li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
				<% else %>
				   	<li><%= link_to "Sign In", :new_user_session  %></li>	
					<li><%= link_to "Register", :new_user_registration %></li>
				<% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <div class="row">
			<div class="span2" id='categories'>
				<%= render "/category/category_list" %>
			</div>
           	<div class="span9">
			<p class="notice"><%= notice %></p>
			<% if alert != nil %>
		    	<p class="alert"><%= alert %></p>
			<% end %>
			<% if flash[:error] != nil %>
		    	<p class="alert alert-error"><%= flash[:error] %></p>
			<% end %>
			<% if flash[:success] != nil %>
		    	<p class="alert alert-success"><%= flash[:success] %></p>
			<% end %>
              <%= yield %>
            </div>
        </div><!--/row-->
      </div><!--/content-->
    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->

  </body>
</html>
